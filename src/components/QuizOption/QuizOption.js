import React from 'react';

const QuizOption = ({ option }) => {
    return (
        <button>
            {
                option
            }
        </button>
    );
};

export default QuizOption;